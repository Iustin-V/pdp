import { StyledPageWrapper, TitleSection } from "./Styles";
import { colors } from "../generalStyle";

export const Courses = () => {
  return (
    <StyledPageWrapper>
      <TitleSection color={colors.primary.base}>Cursuri</TitleSection>
    </StyledPageWrapper>
  );
};
